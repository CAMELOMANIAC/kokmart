# 마트콕 (Martkok) 프로젝트 명세서

AI 기반 대형마트 3사(이마트·홈플러스·롯데마트) 전단 비교 & 스마트 장보기 최적화 플랫폼 **마트콕(Martkok)**의 핵심 기획 및 기술 명세서입니다.

---

## 1. 브랜드 아이덴티티 & 네이밍 체계

- **서비스명**: 마트콕 (영문: `Martkok`)
  - **네이밍 Rationale**: 마트를 콕 집어 최저가를 비교하는 직관적 네이밍 및 타이포그래피 조형미 확보.
- **서비스 정의**: AI 기반 대형마트 3사(이마트·홈플러스·롯데마트) 전단 비교 & 스마트 장보기 최적화 플랫폼.
- **공식 슬로건**: **"콕 집어 비교, 띵한 특가, 바로 띱, 함께 뿜"**

---

## 2. 4-Tab 네비게이션 구조 및 주요 기능

| 탭 이름       | 기능 명칭                        | 핵심 기능 설명                                                                                      |
| :------------ | :------------------------------- | :-------------------------------------------------------------------------------------------------- |
| **[ 🎯 콕 ]** | **홈 & 최저가 비교 (지도 통합)** | 상단 위치 셀렉터/바텀시트로 주변 3사 지점 ON/OFF 토글.<br>100g/개당 단위 환산 최저가 큐레이션 제공. |
| **[ ⚡ 띵 ]** | **전단 핫딜**                    | 목요일 전단 발행 알림 및 1+1, 타임세일, 마감세일 기획전 피드 제공.                                  |
| **[ 🏷️ 띱 ]** | **장바구니 & 분할 계산기**       | 위시리스트 보관 및 **"단일 마트 몰아담기 vs 최적 분할 구매"** 절약액 비교.                          |
| **[ 🍕 뿜 ]** | **대용량 반반 나눔**             | 1+1, 대용량 상품(정육, 베이글, 과일 등)을 동네 이웃과 1/N 소분 매칭하는 가벼운 직거래 커뮤니티.     |

---

## 3. 스마트 비교 & 추천(Smart Tip) 전략

앱의 핵심 가치는 **"무늬만 전단 특가에 속지 않는 진짜 스마트 장보기"**입니다. 신선식품과 공산품의 구분 없이, **실시간 가격 경쟁력과 보관 용이성(유통기한, 소비 속도)**을 종합 평가하여 최적의 구매처를 객관적으로 추천합니다.

- **원칙 1: 진짜 마트 특가 검증 (`MART_BEST` / `MART_RECOMMEND`)**
  - 실시간 온라인/쿠팡 가격 대비 마트 전단가가 확실히 저렴하거나, 오늘 저녁 즉시 소비해야 하는 극소량 신선식품인 경우 현장 구매 추천.
- **원칙 2: 신선식품 포함 온라인/쿠팡 대안 추천 (`COUPANG_TIP` / `COUPANG_BULK`)**
  - **신선식품이라도** 마트 전단가가 온라인/쿠팡(로켓프레시 등)보다 비싸거나 가격 메리트가 없는 경우 쿠팡 대안 추천.
  - 대용량 보관이 용이하고 단가 차이가 큰 공산품/생필품은 쿠팡 대용량 최저가 추천.
  - 객관적인 근거(예: "쿠팡 로켓프레시 100g당 18% 저렴", "온라인 대용량 대비 단가 3,200원 비쌈")를 팁 메시지로 제공.

---

## 4. 데이터 파이프라인 & AI 전단 파싱/추천 명세 (Page-Diff & 2-Stage Pipeline)

- **AI 모델 체계**:
  - **전단지 비전 파싱 및 페이지 재파싱**: **Gemini 3.5 Flash-Lite** (초경량·고속 멀티모달 비전 추론 및 토큰 절약)
  - **웹 검색 그라운딩 & 스마트 구매 판정**: **Groq GPT-OSS-20B API** (`browser_search` 내장 도구 기반 실시간 가격 검색 및 보관성·가격 기반 최적 구매처 큐레이션)
- **핵심 아키텍처**: **2-Tier 실행 아키텍처 (GitHub Actions 주간 공통 전단 배치 + Vercel 실시간 페이지 차분)**
  - **주 1회 공통 전단 배치 (GitHub Actions)**: 매주 목요일 새벽 GitHub Actions가 마트 3사 공통 마스터 전단을 1차 일괄 파싱하여 Supabase DB에 캐시 저장 (실행 제한 6시간 지원으로 Vercel 서버리스 타임아웃 완전 회피).
  - **지점 전단 실시간 차분 (Vercel Serverless)**: 사용자가 특정 지점 전단을 요청하면 Vercel에서 `sharp`로 마스터 전단과 1:1 픽셀 차분하여, **변동이 있는 페이지만 Vercel에서 직접 실시간 단일 페이지 재파싱** (단일 페이지는 상품이 15~20개 내외이므로 Vercel 60초 제한 이내에 안정적 완료).

### 1) 마스터 전단 1차 파싱 (Master Flyer Full-Parse - GitHub Actions 주간 배치)
- **실행 환경**: GitHub Actions Cron 워크플로 (매주 목요일 00:00 KST 자동 실행 또는 수동 트리거)
- **사용 모델**: **Gemini 3.5 Flash-Lite** + **Groq GPT-OSS-20B API (`browser_search` Grounding)**
- 브랜드별(이마트/홈플러스/롯데마트) 대표 지점의 주간 공통 전단 이미지 묶음(4~5장)을 고해상도 그대로 전달.
- **출력 포맷 (TSV 토큰 최적화)**:
  - JSON 문법(키 중복, 괄호)으로 인한 토큰 낭비를 막고 응답 끊김을 원천 차단하기 위해 **TSV(Tab-Separated Values)** 텍스트 포맷으로 출력 유도 (**출력 토큰 60% 이상 절감**).
  - 백엔드에서 탭(`\t`) 단위로 파싱하여 페이지 번호(`pageIndex`)별 마스터 상품 DB 구축 후 Supabase에 영구 캐싱.
  ```text
  페이지번호\t상품명\t할인가\t단위당가격\t단위\t신선식품여부(Y/N)
  1\t국내산 냉장 삼겹살 100g\t1980\t1980\t100g\tY
  1\t다우니 섬유유연제 1L\t7900\t790\t100ml\tN
  ```

### 2) 페이지 단위 차분(Page Diff) 기반 지점별 실시간 갱신 (Vercel Serverless)
- **실행 환경**: Vercel Serverless Function (`POST /api/flyers/sync-branch`)
- **트리거**: 클라이언트 앱에서 특정 지점(예: 홈플러스 청주점, 이마트 역삼점 등)의 전단지 조회를 실시간 요청할 때 동작.
- 지점별로 특정 신선식품 가격이 다르거나 다른 행사 상품으로 교체되는 경우, 박스 단위 땜질이 아닌 **페이지 단위 통교체(Page Replacement)**로 처리합니다.

1. **페이지별 픽셀 차분 연산 (비용 0원, 0.05초 소요)**:
   - 각 지점의 전단 이미지를 Supabase에 캐시된 마스터 전단의 동일 페이지 이미지와 1:1로 픽셀 뺄셈(`diff`) 비교 (`sharp` 라이브러리 활용).
2. **동기화 및 페이지 교체 분기**:
   - **Case A (페이지 일치도 99% 이상 / 차이 없음)**:
     - 100% 동일 전단 페이지로 판정 $\rightarrow$ 마스터 DB 해당 페이지 상품 데이터 즉시 재사용 (Gemini/Groq 호출 0회, 0토큰, 0.05초 반환).
   - **Case B (페이지 내 차이 감지 - 가격 변동 또는 지점 특화 상품 교체)**:
     - 차이가 발생한 **'해당 단일 페이지 1장'만 Vercel에서 직접 실시간 재파싱** (Gemini 3.5 Flash-Lite + Groq 팁 생성).
     - **Vercel 타임아웃 100% 회피**: 전체 4~5장(80~100개 상품)을 돌리는 것이 아니라, 변동된 **단 1개 페이지(상품 15~20개 내외)**만 처리하므로 Vercel 서버리스 60초 제한 시간 이내에 매우 안전하고 쾌적하게 완료됨.
     - 해당 지점의 해당 페이지 상품 목록을 통째로 교체(Replace) $\rightarrow$ **이전 상품이 지워지지 않고 남아있는 '유령 상품(Ghost Product)' 버그 및 좌표 뒤틀림 100% 원천 차단**.

### 3) 2단계 스마트 팁 & 실시간 가격 판정 (Groq GPT-OSS-20B + browser_search Grounding)
1단계에서 정제된 상품 텍스트 데이터를 바탕으로, **Groq GPT-OSS-20B API**가 실시간 웹 검색 그라운딩을 통해 최적의 구매 판단을 내립니다.

1. **실시간 온라인/쿠팡 가격 그라운딩**:
   - 전단 상품의 가격과 단위당 단가를 온라인 최저가 및 쿠팡(로켓배송/로켓프레시) 실시간 가격과 대조.
2. **보관성 및 소비 특성 종합 분석**:
   - 신선식품/공산품 불문, 1~2인 가구 기준 유통기한 내 소진 가능 여부, 보관 편의성, 대용량 단가 차이를 종합 평가.
3. **스마트 팁 생성**:
   - 신선식품이라도 마트가 비싸면 $\rightarrow$ 쿠팡 로켓프레시 대안 팁 제공 (`COUPANG_TIP`).
   - 마트가 온라인보다 확실히 저렴하면 $\rightarrow$ 마트 필구 특가 추천 (`MART_BEST`).
   - 공산품/생필품 중 쿠팡 대용량이 압도적으로 유리하면 $\rightarrow$ 쿠팡 대용량 알뜰 팁 제공 (`COUPANG_BULK`).

### 4) 토큰 및 비용 운용 목표
- **Gemini 3.5 Flash-Lite (비전)**:
  - 마스터 전단 3사 파싱: 주간 약 42,000 토큰
  - 지점별 변동 페이지 재파싱: 페이지당 ~2,800 토큰 × 소수 변동 지점 페이지 = 주간 약 5,000~10,000 토큰
- **Groq GPT-OSS-20B API (텍스트/그라운딩)**:
  - 텍스트 기반 웹 검색 그라운딩 및 팁 생성: 초고속 LPU 추론
- **총 주간 소모량**: **약 5만~6만 토큰 내외**로 일일 무료 할당량(250k)의 25% 수준에서 100% 무료 지속 가능 운영 달성.

### 5) 데이터 구조화 (Structured Output)
`response_schema` 기반 정규화 JSON 반환.

```json
{
  "productName": "다우니 섬유유연제 1L",
  "salePrice": 7900,
  "effectiveUnitPrice": 790,
  "unitMeasure": "100ml",
  "isPerishable": false,
  "smartTip": {
    "tipType": "COUPANG_BULK",
    "badgeText": "대용량 알뜰 팁",
    "tipMessage": "오래 쓰는 세제류는 쿠팡 4L 대용량이 100ml당 40% 이상 저렴해요.",
    "coupangKeyword": "다우니 섬유유연제 4L"
  }
}
```

_(※ `tipType`이 `MART_BEST` 또는 `MART_RECOMMEND`인 경우 `coupangKeyword`는 `null`)_

---

## 5. 전단 외 상품 검색 & 재고 불확실성 대응 (Fallback Protocol)

1. **전단 미포함 일반 상품 검색시**:
   - Gemini 카테고리 매핑을 활용한 유사 카테고리 행사 대체재 자동 추천.
   - 한국소비자원 '참가격' 공공데이터 API 기반 마트 3사 일반 판매가 표시.
   - 마트 행사 전무 시 "쿠팡 로켓배송 최저가" 카드로 사용자 이탈 방지.
2. **오프라인 실시간 재고 대응**:
   - 사전 안내 문구 표시 ("지점 상황에 따라 조기 품절 가능").
   - 커뮤니티 기반 1클릭 제보 UI (`[재고 있어요]` / `[품절됐어요]`).
   - 연속 품절 제보 발생 시 **[조기 품절 주의]** 실시간 뱃지 부착.

---

## 6. 필수 개발 및 코딩 컨벤션 (Strict Coding Rules)

1. **`any` 타입 사용 절대 금지 (Strict TypeScript)**
   - 프로젝트 전반에서 `any` 타입의 사용을 엄격히 금지합니다.
   - 모든 타입은 명확하게 정의(Interface, Type Alias, Generics)하여 사용해야 합니다.
   - 타입을 사전에 확정할 수 없거나 동적인 데이터(예: 외부 API 응답, 에러 객체 등)는 반드시 **`unknown` 타입을 사용**하고, 런타임 타입 가드(Type Narrowing)를 통해 타입을 좁혀서 안전하게 처리해야 합니다.
2. **인라인 스타일(`style={{ ... }}`) 절대 금지 (vanilla-extract 준수)**
   - 컴포넌트 JSX 내부에 인라인 스타일(`style={{ ... }}`) 작성을 전면 금지합니다.
   - 모든 스타일은 Zero-Runtime CSS-in-JS 라이브러리인 **vanilla-extract 전용 모듈(`*.css.ts`)**에 작성하여 클래스로 바인딩해야 합니다.
   - 색상, 여백, 반경 등 디자인 값은 임의의 하드코딩을 지양하고 `theme.css.ts`에 정의된 디자인 시스템 토큰(`vars.*`)을 철저히 재사용하여 일관성을 유지해야 합니다.
3. **테스트 코드 작성 금지 (Jules 전담)**
   - AI(Gemini)는 기능 구현 및 리팩토링에 집중하며, 별도의 테스트 코드(`*.test.ts`, `*.spec.ts`, `*.test.tsx`)를 직접 작성하지 않습니다.
   - 테스트 코드는 Jules가 매일 새벽 배치로 전담 작성하므로, 사용자의 명시적 요청이 없는 한 테스트 코드를 생성하지 않습니다.

